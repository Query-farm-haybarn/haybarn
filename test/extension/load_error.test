# name: test/extension/load_error.test
# description: Check the loading error for typos in extensions
# group: [extension]

require notmingw

require skip_reload

# Haybarn: this used to typo 'motherduck', which was dropped from the extension
# lists (it is a proprietary MotherDuck product, not something Haybarn ships), so
# it no longer appears as a candidate. Use an extension Haybarn does list.
statement error
LOAD postgre_scaner
----
postgres_scanner
